# Common Crate

Provides shared utilities and types used across the Llama workspace.

## Key Public APIs

- `crate::utils` – helper functions for tensor operations.
- `crate::types` – common data structures such as `Tensor`, `Result` aliases.

## Build Instructions

```bash
cd crates/common
cargo build --workspace
```
